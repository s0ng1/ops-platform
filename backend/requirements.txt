# 内网交付需离线打包：pip download -r requirements.txt -d wheels/
fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pydantic>=2.6
pydantic-settings>=2.2
PyJWT>=2.8
cryptography>=42.0
pysnmp==7.1.27
# pysnmp 7.1 的运行时依赖，钉住避免解析到不兼容版本
pyasn1==0.6.4
asyncssh>=2.14
pymysql>=1.1
# Oracle 探针（thin 模式，无需 Instant Client；linux x86_64 平台 wheel 与生产一致）
oracledb==4.0.2
# SQLServer 探针（纯 Python；PyPI 包名 python-tds，导入名 pytds，不用有 C 扩展的 pymssql）
python-tds==1.17.1
psycopg[binary]>=3.1
# 报表导出 Excel（纯 Python，内网可离线装）
openpyxl==3.1.5
# 开发/测试
pytest>=8.0
httpx>=0.27
