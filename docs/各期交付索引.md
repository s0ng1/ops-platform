# 各期交付索引

> 汇总各期实际交付内容的一句话记录，用于快速对齐「哪期做了什么、还差什么」。
> 分期编号为演进后编号（第 1~10 期）；其中第 1/4/5/9 期无独立计划文档，此处补一句话说明。

| 期次 | 一句话交付 | 计划文档 |
|---|---|---|
| 第 1 期 | 基础骨架：FastAPI + Vue3 + Element Plus，JWT + RBAC 三角色、凭据 Fernet 加密、设备 CRUD、IP 段协程扫描、ping/SNMP 监控循环，登录→加设备→探测→扫描→总览冒烟通过 | 无（补此条） |
| 第 2 期 | metrics hypertable、采集调度器、速率计算、SNMP 接口流量与 CPU/内存、Windows SNMP、Linux SSH、MySQL 探针、告警引擎 + SMTP/钉钉/企微通知、WS 推送、设备详情页/告警中心 | `第2期计划.md` |
| 第 3 期 | 拓扑：topo_links 表、LLDP/CDP 邻居发现、G6 v5 深色画布、手工连线/拖拽布局、链路实时流量 | `第3期计划.md` |
| 第 4 期 | 告警收敛（静默窗口/升级）+ 审计日志；报表（可用率/流量 xlsx）+ 大屏；压测 2000 台；docker-compose 全栈打包 + 离线包 | 无（补此条） |
| 第 5 期 | 性能优化（告警引擎批量化、连接池显式配置、labels 过滤下推、cagg 实时聚合、lock_timeout）+ 部署验证；2000 台压测 105.8s→17.6s | 无（补此条） |
| 第 6 期 | 网络运维周边：配置备份（SSH 拉配置 + sha256 去重 + diff）、Syslog/SNMP Trap 接收 → 告警、轻量 IPAM（ARP/MAC 台账 + 白名单） | `第6期计划.md` |
| 第 7 期 | 监控对象扩展：Oracle/SQLServer 探针、应用仿真拨测（http/dns/tcp）、中间件两件套（Nginx stub_status / Redis INFO） | `第7期计划.md` |
| 第 8 期 | 告警增强 + 视图深化：动态基线、告警快照、规则模板、分组子拓扑、宿主机-数据库总线视图、监控循环批量化 | `第8期计划.md` |
| 第 9 期 | 现场接入 + 生产部署（Linux 接入、251/252 开 SNMP 补链路、告警通知闭环、LAN 生产部署）——进行中 | 无（见 `AGENTS.md`） |
| 第 10 期 | 功能补齐 + 安全加固：PostgreSQL 探针、登录防爆破/密码复杂度/默认密钥强制、Redis 口令脱敏、配置备份越权修复、SSRF 防护、WS 票据、Syslog/Trap 白名单、SSH 指纹 TOFU、安全响应头/HTTPS、文档整改 | 无（见 `安全审计报告.md` / `文档审查报告.md`） |

## 说明

- 第 1/4/5/9 期计划文档缺失，属历史演进过程中未单独落盘；实际交付内容见 `AGENTS.md` 进度区（最全的流水账）。
- 第 10 期为本轮「功能补齐 + 安全加固」整改期，对应 `安全审计报告.md` 与 `文档审查报告.md` 中列出的整改项。
